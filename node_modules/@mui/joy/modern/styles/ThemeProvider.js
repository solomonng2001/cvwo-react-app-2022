import * as React from 'react';
import { ThemeProvider as SystemThemeProvider, unstable_styleFunctionSx as styleFunctionSx, useTheme as useSystemTheme } from '@mui/system';
import defaultTheme, { getThemeWithVars } from './defaultTheme';
import { jsx as _jsx } from "react/jsx-runtime";
export const useTheme = () => {
  return useSystemTheme(defaultTheme);
};
const getTheme = themeInput => {
  const theme = getThemeWithVars(themeInput);
  theme.unstable_sx = function sx(props) {
    return styleFunctionSx({
      sx: props,
      theme: this
    });
  };
  return theme;
};
export default function ThemeProvider({
  children,
  theme: themeInput
}) {
  return /*#__PURE__*/_jsx(SystemThemeProvider, {
    theme: themeInput ? getTheme(themeInput) : defaultTheme,
    children: children
  });
}